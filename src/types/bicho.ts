
export interface BichoResult {
  number: string;
  group: number;
  animal: string;
}

export interface AnimalMapping {
  group: number;
  animal: string;
  minNumber: number;
  maxNumber: number;
}
